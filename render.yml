# File: render.yml
services:
  - type: worker
    name: my-telegram-bot-py310
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python3 bot.py"
    autoDeploy: false
    

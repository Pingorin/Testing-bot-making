version: 1
services:
  - type: worker
    name: telegram-polling-bot
    env: python
    dockerfilePath: Dockerfile
    # Updated Start Command
    startCommand: python3 bot.py  # <--- Change applied here
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.10.8
        
